import { SeparadorMilPipe } from './separador-mil.pipe';

describe('SeparadorMilPipe', () => {
  it('create an instance', () => {
    const pipe = new SeparadorMilPipe();
    expect(pipe).toBeTruthy();
  });
});
